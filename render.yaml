services:
  - type: web
    name: sahne-lojistik
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/data
    envVars:
      - key: NODE_VERSION
        value: 20